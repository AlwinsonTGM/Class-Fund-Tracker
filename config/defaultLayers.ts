import type { SegmentedLayer } from '@/types/editor';

/**
 * Default SAM2-cutout photo layers for the Plaza map (stored in `public/maps/plaza/layers/`).
 * Scaled precisely to match the 2200x1500 world map dimensions (scaled down from original 3730x2215 cutout canvas).
 */
export const DEFAULT_PLAZA_LAYERS: SegmentedLayer[] = [];
